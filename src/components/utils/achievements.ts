export const unlockAchievement = (_id: string, _message: string) => {
  // No-op helper for achievement hooks.
  // This exists only to preserve legacy interactive imports without active achievement tracking.
};
